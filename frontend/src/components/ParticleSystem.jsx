import React from 'react';import{motion}from'framer-motion';export const ParticleSystem=()=>(<div className='fixed inset-0 -z-10 pointer-events-none'>{Array.from({length:50}).map((_,i)=>(<motion.div key={i}initial={{x:Math.random()*window.innerWidth,y:Math.random()*window.innerHeight,opacity:1}}animate={{y:-window.innerHeight,opacity:0}}transition={{duration:Math.random()*5+3,repeat:Infinity}}className='absolute w-1 h-1 bg-cyan-400 rounded-full blur-sm'/>))}</div>);export default ParticleSystem;
